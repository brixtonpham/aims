# Test Profile Configuration
spring.application.name=AIMS Backend Test

# H2 In-memory database for testing
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true

# JPA/Hibernate configuration for testing
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Disable caching in tests
spring.cache.type=none

# VNPay Test Configuration (Sandbox)
vnpay.pay-url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.return-url=http://localhost:8080/api/payment/vnpay/return
vnpay.tmn-code=YFW5M6GN
vnpay.secret-key=3RCPI4281FRSY2W6P3E9QD3JZJICJB5M
vnpay.api-url=https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
vnpay.timeout-minutes=15

# Email configuration (disabled for testing)
notification.email.host=localhost
notification.email.port=25
notification.email.username=test@aims.com
notification.email.password=testpass
notification.email.from-address=test@aims.com
notification.email.from-name=AIMS Test

# Logging configuration for tests
logging.level.root=WARN
logging.level.com.aims=INFO
logging.level.org.springframework.test=INFO
logging.level.org.hibernate.SQL=INFO

# Test-specific configuration
aims.frontend.url=http://localhost:3000
aims.test.data.enabled=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging
logging.level.org.springframework=INFO
logging.level.com.vnpay=DEBUG
